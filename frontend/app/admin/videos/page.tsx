"use client";

import { useEffect, useMemo, useState } from "react";
import Link from "next/link";
import Image from "next/image";
import { FiPlus, FiSearch, FiEdit2, FiTrash2, FiVideo, FiPlay, FiRefreshCw } from "react-icons/fi";
import { adminFetch, Video } from "../../../lib/api";

export default function VideosPage() {
  const [videos, setVideos] = useState<Video[]>([]); const [search, setSearch] = useState(""); const [loading, setLoading] = useState(true); const [error, setError] = useState("");
  const load = async () => { setLoading(true); setError(""); const result = await adminFetch<Video[]>("videos"); if (result.success && Array.isArray(result.data)) setVideos(result.data); else setError(result.error || "Unable to load videos."); setLoading(false); };
  useEffect(() => { load(); }, []);
  const filtered = useMemo(() => videos.filter((v) => v.title.toLowerCase().includes(search.toLowerCase())), [videos, search]);
  const remove = async (video: Video) => { if (!window.confirm(`Delete “${video.title}”?`)) return; const result = await adminFetch(`videos/${video.id}`, { method: "DELETE" }); if (result.success) setVideos((items) => items.filter((item) => String(item.id) !== String(video.id))); else setError(result.error || "Unable to delete video."); };
  return <div className="space-y-6"><div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4"><div><h1 className="text-2xl font-extrabold text-white">Videos / Vlog</h1><p className="text-gray-400 text-sm">Manage video content shown on the public site.</p></div><Link href="/admin/videos/new" className="btn btn-primary flex items-center gap-2 text-sm px-5 py-2.5"><FiPlus size={16}/> Add Video</Link></div><div className="flex gap-3"><div className="relative flex-1 min-w-[200px]"><FiSearch className="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400" size={16}/><input placeholder="Search videos..." value={search} onChange={(e) => setSearch(e.target.value)} className="w-full bg-[#0a2540] border border-[#c5a059]/20 rounded-xl pl-10 pr-4 py-2.5 text-sm text-white placeholder-gray-500 focus:border-[#c5a059] focus:outline-none"/></div><button onClick={load} className="btn btn-secondary text-sm flex items-center gap-2" disabled={loading}><FiRefreshCw className={loading ? "animate-spin" : ""}/>Refresh</button></div>{error && <p role="alert" className="rounded-xl border border-red-400/30 bg-red-500/10 p-3 text-sm text-red-300">{error}</p>}<div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4">{loading ? Array.from({length:6}).map((_,i) => <div key={i} className="aspect-video animate-pulse rounded-2xl bg-[#0a2540]"/>) : filtered.length === 0 ? <div className="col-span-full py-16 text-center rounded-2xl border border-[#c5a059]/20 bg-[#0a2540]"><FiVideo className="mx-auto mb-3 text-[#c5a059]/50" size={40}/><p className="font-bold text-white">No videos found</p></div> : filtered.map((video) => { const thumbnail = video.thumbnail || "https://images.unsplash.com/photo-1616486338812-3dadae4b4ace?w=800&q=80"; return <article key={video.id} className="bg-[#0a2540] border border-[#c5a059]/20 rounded-2xl overflow-hidden hover:border-[#c5a059]/40 transition-colors group"><div className="relative aspect-video bg-[#1a3a5c]"><Image src={thumbnail} alt="" fill sizes="400px" className="object-cover opacity-80" unoptimized /><div className="absolute inset-0 flex items-center justify-center"><div className="w-12 h-12 rounded-full bg-[#c5a059] flex items-center justify-center shadow-lg"><FiPlay className="text-[#0a2540] ml-1" size={18}/></div></div>{video.duration && <div className="absolute bottom-2 right-2 bg-black/80 text-white text-xs px-2 py-0.5 rounded">{video.duration}</div>}<span className={`absolute top-2 left-2 rounded bg-black/80 px-2 py-0.5 text-[10px] uppercase font-semibold ${video.published ? "text-[#c5a059]" : "text-amber-300"}`}>{video.published ? "Published" : "Draft"}</span></div><div className="p-4"><h2 className="font-semibold text-white mb-2 line-clamp-2">{video.title}</h2><p className="min-h-10 text-xs text-gray-400 line-clamp-2">{video.description}</p><div className="mt-3 flex items-center justify-end gap-1"><Link href={`/admin/videos/${video.id}`} aria-label={`Edit ${video.title}`} className="p-1.5 rounded-lg hover:bg-[#c5a059]/10 text-gray-400 hover:text-[#c5a059]"><FiEdit2 size={14}/></Link><button onClick={() => remove(video)} aria-label={`Delete ${video.title}`} className="p-1.5 rounded-lg hover:bg-red-500/10 text-gray-400 hover:text-red-400"><FiTrash2 size={14}/></button></div></div></article>; })}</div></div>;
}
